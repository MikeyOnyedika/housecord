const css = { 
    width: "70%",
    height: "2px",
    borderRadius: "var(--border-radius)",
    backgroundColor: "var(--accent-clr)"
}

const Separator = () => {
  return (
    <div style={css}></div>
  )
}

export default Separator